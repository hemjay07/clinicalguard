DATABASE_URL=postgresql://postgres:[PASSWORD]@[HOST]:5432/postgres
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# API only. Comma-separated list of allowed browser origins for CORS.
# Set this on the backend host (Render) to your deployed frontend URL,
# e.g. https://clinicalguard.vercel.app  (localhost dev origins are always allowed).
FRONTEND_ORIGIN=
